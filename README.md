TR = Projenin çalışması için kök dizinde tools/ adında bir klasör oluşturun ve içine android.jar ile gerekli diğer SDK araçlarını yerleştirin.
EN = To make the project work, create a folder named `tools/` in the root directory and place the android.jar file and other necessary SDK tools inside it.
RU = Для корректной работы проекта создайте в корневом каталоге папку с именем `tools/` и поместите в неё файл android.jar и другие необходимые инструменты SDK.
